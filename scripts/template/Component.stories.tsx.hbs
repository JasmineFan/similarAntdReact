import type { Meta, StoryObj } from '@storybook/react';
import { fn } from '@storybook/test';
import  {{componentName}}  from '.';
import { FC} from 'react'



const meta = {
  title: 'Example/{{componentName}}',
  component: {{componentName}},
  parameters: {
    layout: 'centered',
  },
} satisfies Meta<typeof {{componentName}}>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {
  args: {
    type: 'primary',
    children: '{{componentName}}',
  },
};

export const Basic:FC= () =>{
  return(
    <>
    <{{componentName}} type="primary" >Primary {{componentName}}</{{componentName}}>
    <{{componentName}}>Default {{componentName}}</{{componentName}}>
    <{{componentName}} type="dashed">Dashed {{componentName}}</{{componentName}}>
    <{{componentName}} type="text">Text {{componentName}}</{{componentName}}>
    </>
  )
}
